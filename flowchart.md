```mermaid
graph TD;
  P2[Paper 2] --> A[Problem : Enterprise Firewall Anomalies];
  P2 --> C(Algorithm: Reduce Rule Count While Maintaining Security Policy);
  P2 --> D(Logic : Propositional Logic-Based Model for Rule Specification);
 ```
